<div class="container p-4">
  <div class="row">
    <div class="col-md-4 mx-auto">

      <div class="card">
        <div class="card-body">
          <form action="/links/edit/{{links.padecimientoId}}" method="POST">
            <div class="form-group my-4">
              <input
                type="text"
                value="{{links.nombre}}"
                name="nombre"
                class="form-control"
                placeholder="Nombre"
                autofocus
              />
            </div>

            <div class="form-group my-4">
              <textarea
                name="descripcion"
                rows="2"
                class="form-control"
                placeholder="Descripción"
              >{{links.descripcion}}</textarea>
            </div>
            <div class="form-group my-4">
              <button class="btn btn-success btn-block">
                Actualizar
              </button>
            </div>
          </form>
        </div>
      </div>
    </div>

  </div>
</div>